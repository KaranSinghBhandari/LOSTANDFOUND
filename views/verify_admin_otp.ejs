<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Verify Admin OTP</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
  <div class="bg-white p-6 rounded-xl shadow-md w-full max-w-md">
    <h2 class="text-xl font-bold text-center text-blue-600 mb-4">Admin OTP Verification</h2>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="mb-4 text-red-600 text-sm text-center"><%= error %></div>
    <% } %>

    <form method="POST" action="/admin/verify-otp" class="space-y-4">
      <input type="hidden" name="email" value="<%= email %>">
      <label class="block text-sm font-medium">Enter the 6-digit OTP sent to <%= email %>:</label>
      <input type="text" name="otp" required class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400">
      <button type="submit" class="w-full bg-blue-600 text-white py-2 rounded-md hover:bg-blue-700 transition">Verify OTP</button>
    </form>

    <form method="POST" action="/admin/resend-otp" class="text-center mt-4">
      <input type="hidden" name="email" value="<%= email %>">
      <button type="submit" class="text-sm text-blue-600 hover:underline">Resend OTP</button>
    </form>
  </div>
</body>
</html>
